We don't have any source or test files in this package folder.

We just use it to encapsulate the Vue2-related dependencies and will usee source and test files from the `example-library` package (see vite config)