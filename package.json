{
  "name": "@vue-bridge/monorepo",
  "version": "0.1.0",
  "description": "A monorepo template for developing Vue libraries",
  "author": "Thorsten Lünborg <thorsten@linusb.org>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "docs:dev": "cd docs && pnpm start",
    "test": "pnpm run test --filter 'example-library' --parallel --aggregate-output",
    "test:2": "pnpm workspace example-library-vue2 test",
    "test:3": "pnpm workspace example-library test",
    "lint": "pnpm run lint -r --if-present --parallel -- --fix",
    "build": "pnpm build:packages && pnpm build:playground && pnpm build:docs",
    "build:packages": "pnpm run build --filter @vue-bridge/* --parallel --aggregate-output",
    "build:playground": "pnpm run build --filter '{app-,example-library}*'",
    "build:docs": "pnpm workspace docs build",
    "version": "pnpm changeset version",
    "release": "pnpm test && pnpm build:packages && pnpm changeset publish"
  },
  "pnpm": {
    "overrides": {
      "vscode-vue-languageservice": "0.30.1"
    },
    "packageExtensions": {
      "vue-template-compiler": {
        "dependencies": {
          "vue": "^2.6"
        }
      }
    }
  },
  "engines": {
    "node": ">=16",
    "pnpm": ">=6"
  },
  "devDependencies": {
    "@changesets/cli": "^2.18.1",
    "@linusborg/eslint-config": "^0.2.1",
    "eslint": "^8.5.0",
    "prettier": "^2.5.0",
    "tslib": "^2.0.0",
    "typescript": "^4.5.2"
  }
}
